
@model EMR.Models.Patient.Patient


@{
    ViewData["Title"] = "DIAGNOSIS";
}

<h3>Clinical Impressions</h3>

<br /><br />



@using (Html.BeginForm("Treatment", "ENCOUNTER", null))

        {

@Html.HiddenFor(model => model.Name)
@Html.HiddenFor(model => model.Date)
@Html.HiddenFor(model => model.HistoryIllness)
@Html.HiddenFor(model => model.PastHistory)
@Html.HiddenFor(model => model.PhysicalExam)
@Html.HiddenFor(model => model.Tests)

    @Html.HiddenFor(model => model.ChiefComplaints)

    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Diagnosis</legend>

        <div class="editor-field">
            @Html.TextAreaFor(model => model.Diagnosis, 20, 400, new { HtmlAttributes = new { } })
            @Html.ValidationMessageFor(model => model.Diagnosis)
        </div>

    </fieldset>
    <p>
        <input type="submit" value="SUBMIT" />
    </p>
        }
