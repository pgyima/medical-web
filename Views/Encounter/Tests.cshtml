@model EMR.Models.Patient.Patient


@{
    ViewData["Title"] = "TESTS";
}



<br /><br />



@using (Html.BeginForm("Diagnosis", "ENCOUNTER", null))
        {

@Html.HiddenFor(model => model.Name)
@Html.HiddenFor(model => model.Date)
@Html.HiddenFor(model => model.HistoryIllness)
@Html.HiddenFor(model => model.PastHistory)
@Html.HiddenFor(model => model.PhysicalExam)

    @Html.HiddenFor(model => model.ChiefComplaints)

    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Tests</legend>

        <div class="editor-field">
            @Html.TextAreaFor(model => model.Tests, 20, 400, new { HtmlAttributes = new { } })
            @Html.ValidationMessageFor(model => model.Tests)
        </div>

    </fieldset>
    <p>
        <input type="submit" value="SUBMIT" />
    </p>
        }
